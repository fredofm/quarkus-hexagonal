package dxc.microservice.quarkus;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetingResourceIT extends PropResourceTest {

    // Execute the same tests but in native mode.
}